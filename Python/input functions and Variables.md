**input()** it's an functions that allows user to input any data
**Print()**  we use this to print a string(Text)
**Name = parth** -  the word used Name is a variable that hold the memory - parth.
**we used this and previous lesson learning our new test Bot!.**
